#include <windows.h>
/* ======[ DEFINE ]====== */
DWORD sysloadinit = reintepeter_cast<void*> bootloadex();
DWORD reintializer = static_cast<void*> LoadBootIndex();
/* ======[ Initialize ]====== */

#include <cstdint>
